# MLab 实施计划

## 目标

从空工作区交付可直接运行的 AI 工具与导航平台：前端 Vue 3，后端 FastAPI。用户配置一个 OpenAI-compatible 模型 API Key 后，可以完成真实流式对话，并使用助手、任务、文稿、资源、工具导航、记忆和设置功能。

详细产品与架构基线见 `PROJECT_PLAN.md`。本文件用于约束实际实施顺序和完成证据。

## 范围

### 必须交付

1. 单仓工程、Docker Compose、环境变量示例和数据库迁移。
2. 注册、登录、刷新 Token、个人资料与数据隔离。
3. 模型供应商凭据、默认模型、连接测试和密钥加密。
4. 会话、消息、SSE 流式输出、停止生成、重新生成。
5. 助手创建/安装、助手市场与对话绑定。
6. 工具库搜索、分类、收藏、外链与可调用工具区分。
7. 任务、文稿、资源和记忆的持久化操作闭环。
8. 响应式 Vue 界面，覆盖参考图中的首页、市场、工具和设置。
9. 后端测试、前端构建和关键端到端流程验证。
10. README、部署说明、故障排查和 `walkthrough.md`。

### 明确边界

- Midjourney、Suno 等独立产品没有凭据时只作为外部导航，不伪造 API 调用。
- 首期预留订阅领域和 UI，不接真实支付渠道。
- 本地可用 SQLite 降低启动门槛；Docker/生产默认 PostgreSQL。

## 实施顺序

### 阶段 A：工程基线

- 创建前后端目录和构建配置。
- 建立统一配置、日志、异常、数据库 Session 与健康检查。
- 配置 Docker Compose、`.env.example`、格式化与检查工具。

验证：前后端依赖可安装；后端健康检查通过；前端能构建。

### 阶段 B：身份与模型设置

- 实现用户、Refresh Token、Provider Credential、Model Config。
- 实现 Argon2 密码、JWT、凭据加密、掩码返回。
- 完成认证和设置 API 以及前端状态管理。

验证：注册登录、Token 轮换、用户隔离、凭据不明文返回。

### 阶段 C：真实聊天闭环

- 实现会话、消息、统一 Provider 和 SSE 协议。
- 支持历史、多轮上下文、停止、重试和自动标题。
- 完成首页、会话页、Composer 和消息渲染。

验证：Fake Provider 自动测试与真实兼容端点手工验证路径都成立。

### 阶段 D：业务模块

- 助手、工具、任务、文稿、资源、记忆数据与 API。
- 对应 Vue 页面、搜索、筛选、编辑和反馈状态。
- 加入系统种子数据与助手/任务模板。

验证：各模块均存在 CRUD 或安装/收藏闭环，不保留纯静态按钮。

### 阶段 E：质量与交付

- 补齐自动化测试、响应式与可访问性检查。
- 执行构建、Lint、测试、迁移和 Docker 验证。
- 更新 README、任务清单与 walkthrough。

验证：按验收矩阵逐项采集当前代码、测试输出和运行截图作为证据。

## 关键技术决策

- 使用模块化单体，降低中小企业部署和事务管理成本。
- 使用 Vanilla CSS 和 CSS Variables 精确还原视觉，符合项目规则。
- 使用 Web 标准 `fetch` 读取 SSE POST 响应，以便携带 JSON 消息请求。
- Provider 统一抽象为异步事件流，首期实现 OpenAI-compatible 适配器。
- API Token 放在 Authorization Header；Refresh Token 首期使用请求体，生产部署文档提供 HttpOnly Cookie 加固方式。
- 对密钥使用应用级对称加密，主密钥仅由环境变量提供。

## 验收方法

- 后端：pytest 覆盖认证、用户隔离、业务 CRUD、SSE 与错误协议。
- 前端：Vitest 覆盖 Store/组件，`vue-tsc` 与 Vite build 验证类型和构建。
- 集成：启动 API 和前端，Playwright 验证桌面/移动视口关键流程。
- 部署：验证 Compose 配置、迁移、健康检查和全新环境说明。

---

## 2026-07-23 专业化体验完善计划（已完成）

### 目标

在不改变现有 Vue 3 + FastAPI 技术栈和核心业务边界的前提下，将当前界面从“功能原型”完善为可真实使用的专业工作台。重点解决会话无法管理、助手缺少移除入口、账号入口无交互、模型只能随密钥创建一个，以及可见按钮无反馈等问题；视觉调整为灰白主色，黑色仅用于主要操作和选中强调。

### 范围

1. **左侧导航与账号菜单**
   - 将左上角个人信息改为可点击按钮，弹出账号面板。
   - 面板展示头像、名称、邮箱、用量摘要，并提供“个人资料”“应用设置”“订阅与用量”“退出登录”等真实入口。
   - 移除左下角重复的“设置”“退出登录”，保留清晰、单一的账号操作入口。
   - 支持点击外部、按 `Esc` 和路由切换后关闭菜单，补齐键盘焦点与 ARIA 状态。

2. **会话管理**
   - 最近会话支持选中态和完整列表滚动。
   - 每条会话提供“重命名”“归档”“删除”菜单；删除需二次确认，操作后同步当前路由与列表。
   - 会话区提供明确的“新建对话”入口。
   - 对话页右上角菜单复用同一套重命名、归档、删除能力，消除无响应按钮。

3. **助手管理**
   - 点击侧栏助手始终创建一条绑定该助手的新对话并跳转。
   - 助手区提供市场、创建和管理入口。
   - 已安装的公共/系统助手可“移除”；用户自建助手可“编辑”或“删除”，并对破坏性操作做确认。
   - 操作后刷新侧栏和助手市场，避免已删除/移除项继续显示。

4. **多模型配置**
   - 保留“供应商凭据”与“模型配置”两个独立概念：一份凭据可添加多个模型。
   - 模型页新增“添加模型”表单，支持选择凭据、填写模型 ID/别名、温度、最大 Token 和是否默认。
   - 模型列表支持设为默认、修改参数/别名、删除，所有操作提供加载、成功和失败反馈。
   - API 密钥表单明确“初始模型”的含义，保存首个模型后可继续添加同供应商的其他模型。

5. **交互完整性与视觉统一**
   - 审计所有可见按钮、链接和菜单：核心操作必须具备真实结果、禁用态、反馈态或明确导航；无法落地的装饰性按钮直接移除。
   - 重点处理当前无响应的对话“更多”按钮和输入框“智能”按钮。
   - 将大面积纯黑登录介绍区、订阅卡片和助手头像改为灰白层次；黑色仅保留主按钮、发送按钮和关键选中态。
   - 使用现有 Vanilla CSS Design Tokens，统一边框、圆角、阴影、悬停、焦点、空状态和危险操作样式，不引入渐变或 Tailwind。
   - 适配桌面、平板和移动端；菜单在窄屏下保持可触达且不溢出视口。

### 技术方案

- 扩展 `chat` Store，封装会话重命名、归档和删除，并确保列表与当前会话状态一致。
- 将侧栏菜单拆为有明确类型的局部状态和可复用交互，继续使用现有 `/api/v1` 会话、助手安装与用户用量接口。
- 多模型直接复用现有 `POST/PATCH/DELETE /model-configs` 接口；若联调发现引用完整性问题，再补充最小后端修正和测试。
- 不覆盖当前工作区中已有的 API 地址与 Vite 代理改动。
- 使用现有 `lucide-vue-next` 图标，不新增图片资产或占位资源。

### 验证方法

1. `pnpm --dir frontend lint`
2. `pnpm --dir frontend test`
3. `pnpm --dir frontend build`
4. 若涉及后端修正：`python -m pytest backend/tests` 与 `python -m ruff check backend`
5. 启动本地前后端，验证以下关键路径：
   - 账号菜单打开/关闭、设置跳转与退出；
   - 新建、重命名、归档、删除会话；
   - 从侧栏助手创建新对话、移除已安装助手、删除自建助手；
   - 同一凭据添加至少两个模型、切换默认模型、编辑和删除模型；
   - 桌面与移动视口无溢出，所有核心按钮有真实结果或反馈。
6. 生成 `design-qa.md`，将参考截图与实现后的同状态截图对比；P0/P1/P2 问题修复后标记为通过。
7. 更新 `task.md` 与 `walkthrough.md`，记录实际改动、验证结果和仍受环境限制的项目。
